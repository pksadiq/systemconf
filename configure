#!/bin/bash

if [ "$OSTYPE" != "linux-gnu" ]
then
   echo "Sorry, You require GNU/Linux OS to proceed"
   echo "Exiting.."
   exit 1
fi

SCRIPT_DIR="$(pwd)"

. "${SCRIPT_DIR}"/settings.sh
. "${SCRIPT_DIR}"/scripts/gsettings.sh
