# -*- mode: shell-script; -*-

mynetip ()
{
    ROUTES="$(ip route show 2>/dev/null)"

    if [ -z "${ROUTES}" ]
    then
	echo "No Route found"
	exit 1
    fi
    
    dig +short myip.opendns.com @208.67.222.222
}
